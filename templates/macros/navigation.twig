{% macro main(link, name, class = 'nav-link') %}
    <li><a class="{{ class }}" href="{{ link }}" >{{ name }}</a></li>
{% endmacro %}

{% macro footer(link, name) %}
    <li><a href="{{ link }}">{{ name }}</a></li>
{% endmacro %}

